# Notas:
Este es mi primer programa en Node

```
Options:
      --help     Show help                                              [boolean]
      --version  Show version number                                    [boolean]
  -b, --base     Indicate the number to multiply                        [number]
  -l, --listar   Shows the multiplication table of the base             [boolean] [default: false]
  -h, --hasta    Indicate until what number is going to multiply        [number] [default: 10]
```